{
  "id": "compressed-growable-buffer",
  "label": "CompressedGrowableBuffer",
  "includepath": "Misc/CompressedGrowableBuffer.h",
  "includes": [
    "CoreTypes",
    "Array",
    "NameTypes"
  ],
  "functions": [
    {
      "id": "f-compressed-growable-buffer",
      "name": "FCompressedGrowableBuffer",
      "category": "CompressedGrowableBuffer",
      "signature": "FCompressedGrowableBuffer(Max Pending Buffer Size, Compression Format, Compression Flags)",
      "params": [
        {
          "name": "Max Pending Buffer Size",
          "type": "Int"
        },
        {
          "name": "Compression Format",
          "type": "Name"
        },
        {
          "name": "Compression Flags",
          "type": "ECompressionFlags",
          "default": "COMPRESS_None"
        }
      ],
      "returnType": "public :",
      "docs": "Growable compressed buffer. Usage is to append frequently but only request and therefore decompress\nvery infrequently. The prime usage case is the memory profiler keeping track of full call stacks."
    },
    {
      "id": "lock",
      "name": "Lock",
      "category": "CompressedGrowableBuffer",
      "signature": "Lock(Target)",
      "params": [
        {
          "name": "Target",
          "type": "FCompressedGrowableBuffer*"
        }
      ],
      "returnType": "Void",
      "docs": "Locks the buffer for reading. Needs to be called before calls to Access and needs\nto be matched up with Unlock call."
    },
    {
      "id": "unlock",
      "name": "Unlock",
      "category": "CompressedGrowableBuffer",
      "signature": "Unlock(Target)",
      "params": [
        {
          "name": "Target",
          "type": "FCompressedGrowableBuffer*"
        }
      ],
      "returnType": "Void",
      "docs": "Unlocks the buffer and frees temporary resources used for accessing."
    },
    {
      "id": "append",
      "name": "Append",
      "category": "CompressedGrowableBuffer",
      "signature": "Append(Target, Data, Size)",
      "params": [
        {
          "name": "Target",
          "type": "FCompressedGrowableBuffer*"
        },
        {
          "name": "Data",
          "type": "void*"
        },
        {
          "name": "Size",
          "type": "Int"
        }
      ],
      "returnType": "Int",
      "docs": "Appends passed in data to the buffer. The data needs to be less than the max\npending buffer size. The code will assert on this assumption.\n@param\tData\tData to append\n@param\tSize\tSize of data in bytes.\n@return\tOffset of data, used for retrieval later on"
    },
    {
      "id": "access",
      "name": "Access",
      "category": "CompressedGrowableBuffer",
      "signature": "Access(Target, Offset)",
      "params": [
        {
          "name": "Target",
          "type": "FCompressedGrowableBuffer*"
        },
        {
          "name": "Offset",
          "type": "Int"
        }
      ],
      "returnType": "void*",
      "docs": "Accesses the data at passed in offset and returns it. The memory is read-only and\nmemory will be freed in call to unlock. The lifetime of the data is till the next\ncall to Unlock, Append or Access\n@param\tOffset\tOffset to return corresponding data for"
    }
  ]
}