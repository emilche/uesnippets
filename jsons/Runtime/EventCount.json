{
  "id": "event-count",
  "label": "EventCount",
  "includepath": "Async/EventCount.h",
  "includes": [
    "ParkingLot",
    "atomic",
    "type_traits"
  ],
  "functions": [
    {
      "id": "prepare-wait",
      "name": "PrepareWait",
      "category": "EventCount",
      "signature": "PrepareWait(Target)",
      "params": [
        {
          "name": "Target",
          "type": "TEventCount*"
        }
      ],
      "returnType": "TEventCountToken<CounterType>",
      "docs": "Prepare to wait.\nCall this before any logic that must re-execute if the event is notified in the meantime.\n@return A token to pass to one of the wait functions to abort the wait if the event has been notified since."
    },
    {
      "id": "wait",
      "name": "Wait",
      "category": "EventCount",
      "signature": "Wait(Target, Compare)",
      "params": [
        {
          "name": "Target",
          "type": "TEventCount*"
        },
        {
          "name": "Compare",
          "type": "TEventCountToken<CounterType>"
        }
      ],
      "returnType": "Void",
      "docs": "Wait until the event is notified. Returns immediately if notified since the token was acquired.\n@param Compare   A token acquired from PrepareWait() before checking the conditions for this wait."
    },
    {
      "id": "wait-for",
      "name": "WaitFor",
      "category": "EventCount",
      "signature": "WaitFor(Target, Compare, Wait Time)",
      "params": [
        {
          "name": "Target",
          "type": "TEventCount*"
        },
        {
          "name": "Compare",
          "type": "TEventCountToken<CounterType>"
        },
        {
          "name": "Wait Time",
          "type": "FMonotonicTimeSpan"
        }
      ],
      "returnType": "Bool",
      "docs": "Wait until the event is notified. Returns immediately if notified since the token was acquired.\n@param Compare    A token acquired from PrepareWait() before checking the conditions for this wait.\n@param WaitTime   Relative time after which waiting is automatically canceled and the thread wakes.\n@return True if the event was notified before the wait time elapsed, otherwise false."
    },
    {
      "id": "wait-until",
      "name": "WaitUntil",
      "category": "EventCount",
      "signature": "WaitUntil(Target, Compare, Wait Time)",
      "params": [
        {
          "name": "Target",
          "type": "TEventCount*"
        },
        {
          "name": "Compare",
          "type": "TEventCountToken<CounterType>"
        },
        {
          "name": "Wait Time",
          "type": "FMonotonicTimePoint"
        }
      ],
      "returnType": "Bool",
      "docs": "Wait until the event is notified. Returns immediately if notified since the token was acquired.\n@param Compare    A token acquired from PrepareWait() before checking the conditions for this wait.\n@param WaitTime   Absolute time after which waiting is automatically canceled and the thread wakes.\n@return True if the event was notified before the wait time elapsed, otherwise false."
    },
    {
      "id": "notify",
      "name": "Notify",
      "category": "EventCount",
      "signature": "Notify(Target)",
      "params": [
        {
          "name": "Target",
          "type": "TEventCount*"
        }
      ],
      "returnType": "Void",
      "docs": "Notifies all waiting threads.\nAny threads that have called PrepareWait() and not yet waited will be notified immediately\nif they do wait on a token from a call to PrepareWait() that preceded this call."
    }
  ]
}