{
  "id": "manual-reset-event",
  "label": "ManualResetEvent",
  "includepath": "Async/ManualResetEvent.h",
  "includes": [
    "ParkingLot",
    "atomic"
  ],
  "functions": [
    {
      "id": "is-notified",
      "name": "IsNotified",
      "category": "ManualResetEvent",
      "signature": "IsNotified(Target)",
      "params": [
        {
          "name": "Target",
          "type": "FManualResetEvent*"
        }
      ],
      "returnType": "Bool",
      "docs": "Returns true if the event is notified, otherwise false."
    },
    {
      "id": "wait",
      "name": "Wait",
      "category": "ManualResetEvent",
      "signature": "Wait(Target)",
      "params": [
        {
          "name": "Target",
          "type": "FManualResetEvent*"
        }
      ],
      "returnType": "Void",
      "docs": "Wait until the event is notified."
    },
    {
      "id": "wait-for",
      "name": "WaitFor",
      "category": "ManualResetEvent",
      "signature": "WaitFor(Target, Wait Time)",
      "params": [
        {
          "name": "Target",
          "type": "FManualResetEvent*"
        },
        {
          "name": "Wait Time",
          "type": "FMonotonicTimeSpan"
        }
      ],
      "returnType": "Bool",
      "docs": "Wait until the event is notified.\n@param WaitTime   Relative time after which waiting is automatically canceled and the thread wakes.\n@return True if the event was notified before the wait time elapsed, otherwise false."
    },
    {
      "id": "wait-until",
      "name": "WaitUntil",
      "category": "ManualResetEvent",
      "signature": "WaitUntil(Target, Wait Time)",
      "params": [
        {
          "name": "Target",
          "type": "FManualResetEvent*"
        },
        {
          "name": "Wait Time",
          "type": "FMonotonicTimePoint"
        }
      ],
      "returnType": "Bool",
      "docs": "Wait until the event is notified.\n@param WaitTime   Absolute time after which waiting is automatically canceled and the thread wakes.\n@return True if the event was notified before the wait time elapsed, otherwise false."
    },
    {
      "id": "notify",
      "name": "Notify",
      "category": "ManualResetEvent",
      "signature": "Notify(Target)",
      "params": [
        {
          "name": "Target",
          "type": "FManualResetEvent*"
        }
      ],
      "returnType": "Void",
      "docs": "Notifies all waiting threads and leaves the event notified until the next call to Reset()."
    },
    {
      "id": "reset",
      "name": "Reset",
      "category": "ManualResetEvent",
      "signature": "Reset(Target)",
      "params": [
        {
          "name": "Target",
          "type": "FManualResetEvent*"
        }
      ],
      "returnType": "Void",
      "docs": "Resets the event to a non-notified state."
    }
  ]
}