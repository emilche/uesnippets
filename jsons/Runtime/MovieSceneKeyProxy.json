{
  "id": "movie-scene-key-proxy",
  "label": "MovieSceneKeyProxy",
  "includepath": "MovieSceneKeyProxy.h",
  "includes": [
    "ObjectMacros",
    "Interface",
    "MovieSceneSection",
    "MovieSceneChannelHandle",
    "MovieSceneChannel",
    "KeyHandle",
    "GenericPlatformMath",
    "MovieSceneKeyProxy.generated"
  ],
  "functions": [
    {
      "id": "on-proxy-value-changed",
      "name": "OnProxyValueChanged",
      "category": "MovieSceneKeyProxy",
      "signature": "OnProxyValueChanged<ChannelType, ValueType>(Target, In Channel Handle, In Signed Object, In Key Handle, In Out Value, In Out Time)",
      "params": [
        {
          "name": "Target",
          "type": "IMovieSceneKeyProxy*"
        },
        {
          "name": "In Channel Handle",
          "type": "TMovieSceneChannelHandle<ChannelType>"
        },
        {
          "name": "In Signed Object",
          "type": "UMovieSceneSignedObject*"
        },
        {
          "name": "In Key Handle",
          "type": "FKeyHandle"
        },
        {
          "name": "In Out Value",
          "type": "ValueType&"
        },
        {
          "name": "In Out Time",
          "type": "FFrameNumber&"
        }
      ],
      "returnType": "template<ChannelType,ValueType> void",
      "docs": "Implementation function that sets the underlying key time/value to the specified values if possible.\nIf the section is locked, InOutValue and InOutTime will be reset back to the current key's value\n@param InChannelHandle      The channel on which the underlying key value resides\n@param InSection            The section that owns the channel\n@param InKeyHandle          A handle to the key that we are reflecting\n@param InOutValue           Value to assign to the underlying key. If the section is locked, this will receive the existing key's value without changing the underlying key value.\n@param InOutTime            Time to move the underlying key to. If the section is locked, this will receive the existing key's time without changing the underlying time."
    },
    {
      "id": "refresh-current-value",
      "name": "RefreshCurrentValue",
      "category": "MovieSceneKeyProxy",
      "signature": "RefreshCurrentValue<ChannelType, ValueType>(Target, In Channel Handle, In Key Handle, Out Value, Out Time)",
      "params": [
        {
          "name": "Target",
          "type": "IMovieSceneKeyProxy*"
        },
        {
          "name": "In Channel Handle",
          "type": "TMovieSceneChannelHandle<ChannelType>"
        },
        {
          "name": "In Key Handle",
          "type": "FKeyHandle"
        },
        {
          "name": "Out Value",
          "type": "ValueType&"
        },
        {
          "name": "Out Time",
          "type": "FFrameNumber&"
        }
      ],
      "returnType": "template<ChannelType,ValueType> void",
      "docs": "Implementation function that retrieves the underlying key time/value and applies then to the specified value and time parameters. Normally called once per tick.\n@param InChannelHandle      The channel on which the underlying key value resides\n@param InKeyHandle          A handle to the key that we are reflecting\n@param OutValue             (Out) Value to receive the underlying key's value\n@param OutTime              (Out) Time to receive the underlying key time"
    }
  ]
}