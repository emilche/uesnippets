{
  "id": "shared-lock",
  "label": "SharedLock",
  "includepath": "Async/SharedLock.h",
  "includes": [
    "LockTags",
    "AssertionMacros"
  ],
  "functions": [
    {
      "id": "try-lock",
      "name": "TryLock",
      "category": "SharedLock",
      "signature": "TryLock(Target)",
      "params": [
        {
          "name": "Target",
          "type": "TDynamicSharedLock*"
        }
      ],
      "returnType": "Bool",
      "docs": "Try to lock the associated mutex in shared mode. This lock must have a mutex and must not be locked."
    },
    {
      "id": "lock",
      "name": "Lock",
      "category": "SharedLock",
      "signature": "Lock(Target)",
      "params": [
        {
          "name": "Target",
          "type": "TDynamicSharedLock*"
        }
      ],
      "returnType": "Void",
      "docs": "Lock the associated mutex in shared mode. This lock must have a mutex and must not be locked."
    },
    {
      "id": "unlock",
      "name": "Unlock",
      "category": "SharedLock",
      "signature": "Unlock(Target)",
      "params": [
        {
          "name": "Target",
          "type": "TDynamicSharedLock*"
        }
      ],
      "returnType": "Void",
      "docs": "Unlock the associated mutex in shared mode. This lock must have a mutex and must be locked."
    },
    {
      "id": "owns-lock",
      "name": "OwnsLock",
      "category": "SharedLock",
      "signature": "OwnsLock(Target)",
      "params": [
        {
          "name": "Target",
          "type": "TDynamicSharedLock*"
        }
      ],
      "returnType": "Bool",
      "docs": "Returns true if this lock has its associated mutex locked."
    },
    {
      "id": "bool",
      "name": "bool",
      "category": "SharedLock",
      "signature": "bool(Target)",
      "params": [
        {
          "name": "Target",
          "type": "TDynamicSharedLock*"
        }
      ],
      "returnType": "operator",
      "docs": "Returns true if this lock has its associated mutex locked."
    }
  ]
}